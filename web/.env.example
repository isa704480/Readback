# Origin of the Readback API. Leave unset to use http://localhost:8000, which is
# where the server agent is building. Set to an empty string to go same-origin
# and route through the dev proxy in vite.config.ts instead.
VITE_READBACK_API=http://localhost:8000
