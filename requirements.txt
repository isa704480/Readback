# Readback — server runtime. The DIRECT dependencies, each with the reason it
# is here. Render does NOT install this file any more: it installs
# requirements.lock, which is compiled from this one and carries every
# transitive package pinned with its artefact hashes. Change a version here and
# regenerate the lock with the command in its header, or the deploy keeps the
# resolution it already had.
fastapi==0.141.1
uvicorn[standard]==0.34.0
starlette==1.6.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
httpx==0.28.1
websockets==17.0.1
# Postgres driver for Render/Neon. Not installed on the dev laptop, which runs
# SQLite; db.py's normalise_database_url() rewrites postgres:// to
# postgresql+psycopg:// so this is the one that has to be present in prod.
psycopg[binary]==3.2.3
